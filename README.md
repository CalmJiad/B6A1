<p align="center">
  <img src="https://raw.githubusercontent.com/remojansen/logo.ts/master/ts.png" width="120" />
</p>

<h1 align="center">🧠 TypeScript Insights</h1>

> _"Deep dives into TypeScript basics to boost the code quality and scale your team projects efficiently."_

---

# TypeScript-এ Interface এবং Type Alias এর পার্থক্য

TypeScript শিখতে গিয়ে দেখা যায়, টাইপ ডিফাইন করার জন্য দুইটি প্রধান উপায় আছে: interface এবং type alias। প্রথমদৃষ্টিতে এগুলো অনেকটা একই রকম মনে হতে পারে, কিন্তু কিছু গুরুত্বপূর্ণ পার্থক্য আছে। আমি TypeScript-এর ডকুমেন্টেশন এবং কমিউনিটি রিসোর্স থেকে কিছু তথ্য সংগ্রহ করেছি। এখানে সেগুলোকে সংক্ষেপে ব্যাখ্যা করা হলো।

## ১. Interface এবং Type উভয়ই অবজেক্টের আকৃতি (object shape) ডিফাইন করতে পারে

interface Person {
name: string;
age: number;
}

type PersonType = {
name: string;
age: number;
};

উদাহরণ থেকে দেখা যায়, Person এবং PersonType দুটোই একই রকম অবজেক্ট ডিফাইন করছে। দুটোই name এবং age প্রপার্টি রাখে। অর্থাৎ, এখানে কোন পার্থক্য নেই।

## ২. Interface এবং Type উভয়ই এক্সটেন্ড করা যায়, তবে ভিন্নভাবে:

```typescript
interface Person {
  name: string;
  age: number;
}

interface Employee extends Person {
  employeeId: number;
}

type PersonType = {
  name: string;
  age: number;
};

type EmployeeType = PersonType & {
  employeeId: number;
};
```

interface ব্যবহার করে এক্সটেন্ড করতে extends কীওয়ার্ড ব্যবহার হয়। type alias এক্সটেন্ড করতে & (intersection) ব্যবহার করতে হয়। এটি TypeScript-এ interface এবং type-এর মধ্যে একটি মূল পার্থক্য।

## ৩. Interface পুনঃসংজ্ঞায়িত (redefine) করা যায়, type পুনঃসংজ্ঞায়িত করা যায় না:

```typescript
interface Person {
  name: string;
  age: number;
}

interface Person {
  employeeId: number;
}

type PersonType = {
  name: string;
  age: number;
};

type PersonType = {
  employeeId: number; // ❌ Error: duplicate identifier
};
```

Interface কে পুনঃসংজ্ঞায়িত করা যায় এবং নতুন প্রপার্টি যোগ করা যায়। Type alias পুনঃসংজ্ঞায়িত করা যায় না। তাই interface একটু ফ্লেক্সিবল।

## ৪. Type শুধু primitive, tuple, union, intersection এর জন্য ব্যবহার করা যায়:

```typescript
type StringOrNumber = string | number;
type Point = [number, number];

type Person = {
  name: string;
  age: number;
};

type Employee = Person & {
  employeeId: number;
};
```

Type alias দিয়ে primitive type, tuple, union, intersection তৈরি করা যায়। Interface দিয়ে এটি করা যায় না। অর্থাৎ type কিছু ক্ষেত্রে আরও পাওয়ারফুল, interface কিছুটা সীমাবদ্ধ।

## উপসংহার

interface এবং type alias উভয়ই TypeScript-এ টাইপ ডিফাইন করার জন্য গুরুত্বপূর্ণ টুল। interface একটু বেশি ফ্লেক্সিবল এবং পুনঃসংজ্ঞায়িত করা যায়, যেখানে type alias আরও পাওয়ারফুল এবং বিভিন্ন ধরনের টাইপ ডিফাইন করতে পারে। প্রজেক্টের প্রয়োজনীয়তা অনুযায়ী সঠিক টুলটি বেছে নেওয়া উচিত।

---

# TypeScript এ any, unknown, এবং never টাইপের পার্থক্য

TypeScript আমাদের জাভাস্ক্রিপ্টের চেয়ে আরও বেশি type safety দেয়। তবে অনেক সময় এমন টাইপের ব্যবহার দরকার হয় যা একটু flexible। TypeScript এ তিনটি বিশেষ টাইপ আছে – any, unknown, এবং never। চলুন এগুলো step by step দেখাই।

## ১. any টাইপ

any টাইপ এমন একটি টাইপ যা যেকোনো ধরনের value নিতে পারে। এটি ব্যবহার করলে TypeScript type-checking চেক করে না। any টাইপ দিলে TypeScript behave করে জাভাস্ক্রিপ্টের মতো, কিন্তু এর ফলে type safety চলে যায়।

```typescript
let a: any = "hello";
a = 42; // কোনো সমস্যা নেই
a = true; // আবারও কোনো সমস্যা নেই
```

এখানে আমরা string, number, boolean সবই assign করতে পারছি। TypeScript কোনো warning দিচ্ছে না। তাই any ব্যবহারের সময় সাবধান হওয়া উচিত।

## ২. unknown টাইপ

unknown হলো any এর মতো flexible, কিন্তু safer। এর মানে হলো, আমরা যেকোনো value assign করতে পারি, কিন্তু ব্যবহার করার আগে type check করতে হবে। এটি আমাদের type safety বজায় রাখতে সাহায্য করে।

```typescript
let b: unknown = "hello";
b = 42; // assign করতে পারি

// কিন্তু ব্যবহার করার আগে type check করতে হবে
if (typeof b === "string") {
  console.log(b.toUpperCase()); // এখন safe
}
```

এখানে আমরা দেখতে পাচ্ছি যে, unknown টাইপের ভেরিয়েবল b তে যেকোনো value assign করা যায়, কিন্তু ব্যবহার করার আগে type check করতে হয়। এটি type safety বজায় রাখতে সাহায্য করে।

## ৩. never টাইপ

never টাইপ এমন জিনিসের জন্য যা কখনো ঘটবে না। যেমন: কোনো function যা always throw করে বা infinite loop। এই টাইপ সাধারণত function return type হিসেবে ব্যবহার হয়, যা কোনো value return করবে না।

```typescript
function error(): never {
  throw new Error("Something went wrong");
}

function infiniteLoop(): never {
  while (true) {
    // কখনো শেষ হবে না
  }
}
```

এখানে error ফাংশন কখনোই কোনো value return করবে না, কারণ এটি সবসময় exception throw করে। একইভাবে, infiniteLoop ফাংশন কখনোই শেষ হবে না।

## উপসংহার

any, unknown, এবং never টাইপের মধ্যে পার্থক্য বুঝতে পারা TypeScript এ type safety বজায় রাখতে সাহায্য করে। any টাইপ ব্যবহার করার সময় সাবধান হওয়া উচিত, কারণ এটি type safety হারিয়ে দেয়। unknown টাইপ ব্যবহার করলে type safety বজায় থাকে, এবং never টাইপ এমন জিনিসের জন্য যা কখনো ঘটবে না। সঠিক টাইপ বেছে নিয়ে কোডিং করলে TypeScript এর সুবিধা পুরোপুরি উপভোগ করা যায়।
